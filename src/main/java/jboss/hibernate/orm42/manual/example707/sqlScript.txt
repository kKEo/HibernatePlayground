    create table Customer (
        id bigint not null,
        primary key (id)
    )
 
    create table orders (
        id bigint generated by default as identity,
        numer varchar(255),
        customer_id bigint,
        primary key (id)
    )